/**
 * Placeholder until real auth/User accounts exist (Phase 3 in ROADMAP.md).
 * Single hardcoded user so Client/Job/TimeEntry records have a stable user_id to key off.
 */
export const CURRENT_USER_ID = 'local-user';
